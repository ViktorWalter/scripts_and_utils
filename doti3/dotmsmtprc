defaults
auth on
tls on
# Same as sslcacertfile in ~/.offlineimaprc
tls_trust_file /etc/ssl/certs/ca-certificates.crt

account fel
host imap.feld.cvut.cz
user waltevik@fel.cvut.cz
# The value of `from` is only used when you're not using sup.
# But it is necessary if you're testing things out.
from Viktor Walter
passwordeval python -c "import keyring; print keyring.get_password('fel', 'personal')"
port 25

account outlook
host smtp-mail.outlook.com
user viktor_walter@outlook.com
# The value of `from` is only used when you're not using sup.
# But it is necessary if you're testing things out.
from Viktor Walter
passwordeval python -c "import keyring; print keyring.get_password('outlook', 'personal')"
port 587

account gmail
host smtp.gmail.com
user ciernobiely@gmail.com
# The value of `from` is only used when you're not using sup.
# But it is necessary if you're testing things out.
from Viktor Walter
passwordeval python -c "import keyring; print keyring.get_password('gmail', 'personal')"
port 587

account default : fel
